// const fs = require("fs");
// const { array } = require("./resArr");

// const result = array.filter((item) => item.category === "Dessert");

// fs.appendFile("hello.json", JSON.stringify(result), function (error) {
//   if (error) throw error; // если возникла ошибка
// });

// const mainDish = require("./mainFoodBase.json");

// const result = mainDish.map((obj) => ({
//   ...obj,
//   price: (Math.random() * (45 - 20) + 20).toString().slice(0, 5),
// }));

// console.log(result);

// fs.appendFile("foodCollection.json", JSON.stringify(result), function (error) {
//   if (error) throw error; // если возникла ошибка
// });
